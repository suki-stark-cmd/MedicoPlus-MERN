{
    "version": "1.0.0",
    "name": "medicoplus-mern",
    "description": "MedicoPlus MERN - Monorepo with backend API and three frontend apps",
    "devDependencies": {
        "concurrently": "^9.2.4"
    },
    "scripts": {
        "dev:doctor": "npm run dev --prefix doctor",
        "dev:admin": "npm run dev --prefix admin",
        "dev:frontend": "npm run dev --prefix frontend",
        "dev:backend": "npm run server --prefix backend",
        "dev": "concurrently \"npm run server --prefix backend\" \"npm run dev --prefix frontend\" \"npm run dev --prefix doctor\" \"npm run dev --prefix admin\""
    },
    "private": true
}
